# dependencies
node_modules/

# build output
dist/

# generated standalone preview (reproducible via `npm run preview:standalone`)
index-preview.html

# local env / secrets — tokens are user-entered at runtime, never committed
.env
.env.*
*.local

# Claude Code local-only settings (personal permission allowlist)
.claude/settings.local.json

# OS / editor
.DS_Store
*.swp
.vscode/
.idea/

# OPSEC: never commit coordinate-bearing data or private network files
*.private.kml
*.private.geojson
/private/
